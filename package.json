{
  "name": "burak-backend",
  "version": "1.0.0",
  "description": "this is burak project backend",
  "license": "ISC",
  "author": "MIT",
  "type": "commonjs",
  "main": "src/server.ts",
  "scripts": {
    "start": "ts-node src/server.ts",
    "start:dev": "nodemon --exec ts-node src/server.ts",
    "build": "tsc",
    "train": "nodemon train.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "moment": "^2.29.4",
    "ts-node": "^10.9.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.2.2"
  }
}
